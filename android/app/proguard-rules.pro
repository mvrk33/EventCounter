# Daymark release rules.
# Keep this file minimal and additive. Flutter plugin rules are applied automatically.

# Keep SourceFile/LineNumber for readable crash stack traces in release builds.
-keepattributes SourceFile,LineNumberTable

